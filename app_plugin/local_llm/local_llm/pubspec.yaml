name: app_local_llm
description: Unified API for local LLM inference on Android and iOS
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0
  flutter: ^3.24.0

dependencies:
  flutter:
    sdk: flutter
  app_local_llm_platform_interface: any
  app_local_llm_android: any
  app_local_llm_ios: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
