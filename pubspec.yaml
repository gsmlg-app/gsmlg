name: gsmlg
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.12.0 <4.0.0"

workspace:
  - app_lib/database
  - app_lib/theme
  - app_lib/locale
  - app_lib/provider
  - app_lib/logging
  - app_lib/secure_storage
  - app_lib/chat
  - app_lib/tts_dataset
  - app_lib/isolate_orchestrator
  - app_bloc/theme
  - app_bloc/auth
  - app_bloc/whois
  - app_bloc/whois_history
  - app_bloc/bluetooth
  - app_bloc/camera
  - app_bloc/domain
  - app_bloc/github
  - app_bloc/vultr
  - app_bloc/accounts
  - app_bloc/chat
  - app_bloc/monitor
  - app_bloc/tts_dataset
  - app_widget/adaptive
  - app_widget/artwork
  - app_widget/feedback
  - app_widget/web_view
  - app_widget/components
  - app_widget/data_list
  - app_widget/world_map
  - app_plugin/client_info/client_info
  - app_plugin/client_info/client_info_platform_interface
  - app_plugin/client_info/client_info_android
  - app_plugin/client_info/client_info_ios
  - app_plugin/client_info/client_info_linux
  - app_plugin/client_info/client_info_macos
  - app_plugin/client_info/client_info_windows
  - app_plugin/local_llm/local_llm
  - app_plugin/system_metrics/system_metrics
  - app_plugin/system_metrics/system_metrics_platform_interface
  - app_plugin/system_metrics/system_metrics_android
  - app_plugin/system_metrics/system_metrics_ios
  - app_plugin/system_metrics/system_metrics_linux
  - app_plugin/system_metrics/system_metrics_macos
  - app_plugin/system_metrics/system_metrics_windows
  - app_plugin/local_llm/local_llm_platform_interface
  - app_plugin/local_llm/local_llm_android
  - app_plugin/local_llm/local_llm_ios
  - app_api/route53
  - app_api/cloudflare_dns
  - app_api/github
  - app_api/vultr_api
  - app_api/openai_compatible
  - app_api/anthropic_compatible
  - third_party/gsmlg_whois
  - third_party/lib_litert_lm
  - app_form/camera_settings
  - app_lib/ip_db
  - monitor_models
  - monitor_agent

dependencies:
  flutter:
    sdk: flutter

  logging: ^1.3.0

  go_router: ^17.2.1

  bloc: ^9.2.0
  flutter_bloc: ^9.0.0
  equatable: ^2.0.8

  shared_preferences: ^2.5.5
  path_provider: ^2.1.5
  path: any

  cupertino_icons: ^1.0.9
  flutter_svg: ^2.2.4
  lottie: ^3.3.3

  duskmoon_ui: ^1.6.0
  duskmoon_adaptive_scaffold: ^1.6.0
  flutter_slidable: ^4.0.0
  file_picker: ^8.3.7
  intl: any
  flutter_blue_plus: ^2.2.1
  camera: ^0.12.0+1
  camera_macos: any

  crypto: any
  dio: any
  lib_llama_cpp: any
  multicast_dns: any
  url_launcher: ^6.3.1

  # workspace packages
  cloudflare_dns: any
  route53: any
  github_api: any
  vultr_api: any
  openai_compatible: any
  anthropic_compatible: any
  app_database: any
  app_theme: any
  app_locale: any
  app_provider: any
  theme_bloc: any
  auth_bloc: any
  whois_bloc: any
  whois_history_bloc: any
  bluetooth_bloc: any
  camera_bloc: any
  domain_bloc: any
  github_bloc: any
  vultr_bloc: any
  accounts_bloc: any
  app_artwork: any
  app_client_info: any
  app_system_metrics: any
  app_feedback: any
  app_adaptive_widgets: any
  app_logging: any
  app_secure_storage: any
  isolate_orchestrator: any
  app_web_view: any
  app_components: any
  app_data_list: any
  app_world_map: any
  gsmlg_whois: any
  duskmoon_form: ^1.6.0
  camera_settings_form: any
  ip_db: any
  app_chat: any
  tts_dataset: any
  app_local_llm: any
  chat_bloc: any
  monitor_bloc: any
  tts_dataset_bloc: any
  monitor_models: any
  monitor_agent: any

  # data visualization
  duskmoon_visualization: ^1.6.0
  record: ^6.2.0
  wakelock_plus: ^1.5.2
  background_downloader: ^9.5.5

dependency_overrides:
  flutter_blue_plus_darwin:
    path: third_party/flutter_blue_plus_darwin

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.30.0

  flutter_lints: ^6.0.0

  build_runner: ^2.13.1

  melos: ^7.5.1
  mason: ^0.1.1
  mason_cli: ^0.1.1
  dependency_validator: ^5.0.2

flutter:
  uses-material-design: true

  assets:
    - assets/icon/
    - assets/world.geo.json

  fonts:
    - family: GSMLG
      fonts:
        - asset: assets/fonts/GSMLG.ttf

# generate icon with `dart run flutter_launcher_icons:main``
flutter_launcher_icons:
  image_path: "app_widget/artwork/assets/icon/app-icon.png"
  android: true
  ios: true
  min_sdk_android: 21
  windows:
    generate: true
    icon_size: 48 # min:48, max:256, default: 48
  macos:
    generate: true

melos:
  name: gsmlg
  command:
    version:
      # Only allow versioning to happen on main branch.
      branch: main
      # Generates a link to a prefilled GitHub release creation page.
      releaseUrl: true

  scripts:
    prepare:
      description: Prepare project with `bootstrap`, `build_runner build` and `gen-l10n`
      steps:
        - melos bootstrap
        - melos run gen-l10n
        - melos run build-runner

    build-runner:
      run: dart run build_runner build --delete-conflicting-outputs
      exec:
        orderDependents: true
      description: Run `dart/flutter analyze` for all packages.

    analyze:
      description: Run `dart/flutter analyze` for all packages.
      steps:
        - melos run analyze:dart
        - melos run analyze:flutter

    analyze:dart:
      run: dart analyze --fatal-warnings
      exec:
        concurrency: 1
        orderDependents: true
      packageFilters:
        flutter: false
      description: Run `dart analyze` for non-Flutter packages.

    analyze:flutter:
      run: flutter analyze --fatal-warnings
      exec:
        concurrency: 1
        orderDependents: true
      packageFilters:
        flutter: true
      description: Run `flutter analyze` for Flutter packages.

    fix:
      run: dart fix . --apply
      exec:
        orderDependents: true
      description: Run `dart fix` for all packages.

    fix-dry-run:
      run: dart fix . --dry-run
      exec:
        orderDependents: true
      description: Run `dart fix` in dry-run mode for all packages.

    format:
      run: dart format .
      exec:
        orderDependents: true
      description: Run `dart format` for all packages.

    format-check:
      run: dart format . --set-exit-if-changed
      exec:
        orderDependents: true
      description: Run `dart format` checks for all packages.

    upgrade:
      run: dart pub upgrade --major-versions
      exec:
        orderDependents: true
      description: Run `flutter pub upgrade` update dependencies in all packages.

    outdated:
      run: dart pub outdated
      exec:
        orderDependents: true
      description: Run `flutter pub outdated` check outdated dependencies in all packages.

    validate-dependencies:
      run: dart run dependency_validator
      description: Run `dart run dependency_validator` check dependencies usage.

    test:
      run: melos exec --order-dependents --fail-fast flutter test || true
      description: Run Flutter tests for all packages (ignores packages without tests).
      steps:
        - melos run test:dart
        - melos run test:flutter

    test:dart:
      run: dart test
      exec:
        orderDependents: true
        fail-fast: true
      packageFilters:
        flutter: false
        dirExists: test

    test:flutter:
      run: flutter test
      exec:
        orderDependents: true
        fail-fast: true
      packageFilters:
        flutter: true
        dirExists: test

    gen-l10n:
      run: flutter gen-l10n
      exec:
        orderDependents: true
      packageFilters:
        flutter: true
        fileExists: l10n.yaml
      description: Generate localization files from ARB files.
