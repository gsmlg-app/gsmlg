name: tts_dataset
description: Core TTS dataset models, repository, validation, and Qwen export support.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.41.0"

dependencies:
  archive: ^4.0.7
  crypto: any
  drift: any
  equatable: ^2.0.8
  flutter:
    sdk: flutter
  path: any
  app_database: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
