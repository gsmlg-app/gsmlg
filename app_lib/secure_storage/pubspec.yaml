name: app_secure_storage
version: 1.0.0
description: Secure storage API for saving user secrets using platform-native secure storage.
publish_to: "none"

environment:
  sdk: ">=3.8.0 <4.0.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  # 10.3.0+ fixes macOS usesDataProtectionKeychain option propagation.
  flutter_secure_storage: ^10.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
