name: app_world_map
description: World map widget with location marker support using GeoJSON data.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.7.0
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  data_visualization: any
  duskmoon_theme: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
